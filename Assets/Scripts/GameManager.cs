using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    public float coinsForLevel = 2f;
    // Start is called before the first frame update
   
}
